"""hextetra-viewer：把字节流按 64 进制（两位八进制 + 标准 Base64）转储的查看器。

模块划分：

- ``core``：纯算法，字节与 6 bit 单元之间的换算，不涉及 I/O 与显示
- ``render``：把 ``core`` 的行结构渲染成终端文本（纯文本 + 可选 ANSI 颜色）
- ``document``：数据源（文件 mmap / 标准输入），预留将来的编辑接缝
"""

__version__ = "0.1.0"
